# Desktop dual-subscription E2E report — 2026-08-25

This report records the completed signed-app verification for the
`compat/chatgpt-26.818` work on the `kellanxu` fork.

| Item | Verified value |
| --- | --- |
| Platform | macOS on Apple silicon |
| Official ChatGPT version | `26.818.41509` |
| Official bundle build | `6962` |
| Official `app.asar` SHA-256 | `8eb91bd9efbf9a4dd04b9b0afdbfcb4e0bab5da18c1919ad74ca327c00c7e791` |
| Connected subscriptions | 2 |
| Router control endpoint | `127.0.0.1:48123` |

## Passed

- The independently signed Router app and Computer Use helper passed strict,
  deep code-signature verification and launched in normal Desktop mode.
- Primary and one secondary ChatGPT subscription were connected through the
  Desktop account flow. A stale disconnected placeholder was ignored by the
  routing pool and preserved for user review.
- A new Desktop chat was assigned to the secondary subscription and returned
  `ROUTER_E2E_STEP_1_OK` from a real model turn.
- A second turn in the same thread remained on the secondary subscription and
  returned `ROUTER_E2E_STEP_2_OK`, proving sticky ownership.
- The token-authenticated test preview marked only the secondary subscription
  depleted. The Router staged the rollout under the Primary Codex home,
  preserved the original rollout filename required by app-server discovery,
  registered the same thread ID, and moved ownership to Primary.
- Primary continued the same Desktop thread and returned another
  `ROUTER_E2E_STEP_2_OK`; the response was visible in the app and persisted in
  Primary's rollout.
- The rate-limit preview was cleared. After restarting normally, the private UI
  test bridge on `127.0.0.1:48124` was closed while the production control
  endpoint remained healthy.
- The official ChatGPT `app.asar` and `Info.plist` hashes matched the pre-build
  baseline, confirming that `/Applications/ChatGPT.app` was not modified.
- `go test ./...`, `go vet ./...`, JavaScript syntax checks, Python compilation,
  shell syntax, release metadata, and `git diff --check` all passed.

## Screenshot privacy

The README E2E screenshot comes from this run. Project and task names in the
left sidebar are covered with a strong pixel mosaic before publication. The
central E2E conversation is not content-edited.

## Scope note

The verification proves one live secondary-to-Primary failover while preserving
the logical thread ID. Repeated round-trip migration back to a previously used
account was not part of this run.
