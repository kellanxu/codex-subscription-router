# Desktop dual-subscription E2E report — 2026-08-26

This report records the signed-app compatibility and installed-app verification
for the official ChatGPT Desktop build approved on 2026-08-26.

| Item | Verified value |
| --- | --- |
| Platform | macOS on Apple silicon |
| Official ChatGPT version | `26.818.61809` |
| Official bundle build | `7019` |
| Official `app.asar` SHA-256 | `76bbcdc2a4a2d77cfe03904a6537d0a655f9892f27a8925e3a6c7b613801d4cf` |
| Installed patched `app.asar` SHA-256 | `0b77a20548161744aeaf787f5a7818ebb7ab887b5adf893177eb39c2f1ff3559` |
| Patch source commit | `e28a8069bf4a3381fad074a1870c2c0c2e8b2b56` |
| Connected subscriptions | 2 |
| Signing | Apple Development, Team ID `BD6RV5Q85G` |
| Installed E2E evidence | `~/.codex-mux/e2e/20260826-110521` |

## Passed

- The Router app and its Computer Use helper passed strict, deep code-signature
  verification before the installed-app test.
- The Desktop account menu displayed two connected subscriptions with pooled
  and per-account usage.
- Profile account selection and Settings → Plugins account selection both
  switched to the second subscription.
- With Primary previewed as depleted, a new real model turn routed to the
  second subscription and returned the exact expected reply. A second turn in
  the same thread stayed on that subscription.
- With the second subscription previewed as depleted, a new real model turn
  routed to Primary and returned the exact expected reply. A second turn in the
  same thread stayed on Primary.
- The native Usage sheet opened successfully and its subscription selector
  switched to the second subscription.
- The same complete suite passed first on a temporary compatibility build and
  then on the installed app. Both runs restored normal Desktop mode afterward.
- The private E2E bridge on `127.0.0.1:48124` was closed after the run. The
  production control endpoint on `127.0.0.1:48123` remained healthy.
- The official source fingerprint still matched the approved version, build,
  and ASAR hash after installation.
- Go tests and vet, renderer syntax checks, Python compilation and guard tests,
  shell syntax, release metadata, and `git diff --check` passed.

## Evidence handling

The local evidence directory contains `result.json`, the Router log, and 13
full-resolution screenshots covering every UI and routing checkpoint. These
screenshots can expose local sidebar task names, so they remain local and are
not committed. Public README screenshots use separately curated images with
private task names removed or mosaicked.

## Scope

This run proves both routing directions and sticky ownership on each account,
plus the account menu, Profile, Plugins, and Usage selectors on build `7019`.
It does not claim compatibility with an unlisted future ChatGPT Desktop build.
